# **P 到底是什么意思？（严格定义版）**

你看到的符号是：

$$ P_k = (P_k^A,; P_k^B) $$

它**不是一个三角形**，  
**也不是一个单独的 patch**，  
而是——

> **一个“跨两个 object 的局部接触候选对（contact patch pair）”**

---

## 一句话版定义（你可以记这个）

> **$P_k$ 表示：  
> Object A 上的一小片三角面集合，与  
> Object B 上的一小片三角面集合，  
> 在几何上被判定为“可能属于同一公共接触区域”的一对 patch。**

---

# **拆开来看：$P_k^A$ 和 $P_k^B$ 分别是什么？**

### $P_k^A$

- Object A 上的一个 **局部三角面集合**
    
- 来源：
    
    - BVH 配对
        
    - gap + normal 过滤
        
    - Step 2 seed 之后形成
        
- 语义：
    
    - “A 上**可能参与公共接触**的那一小块表面”

### $P_k^B$

- Object B 上与之对应的 **局部三角面集合**
    
- 与 $P_k^A$ 是：
    
    - **一一配对**
        
    - **共同参与支持度计算**
        

---

## ⚠️ 非常重要的一点（很多人会误解）

> **patch ≠ 只存在于一个 object 上**

在你的问题里，**patch 是“成对存在”的**：

- 单独的 $P_k^A$ 没有意义
    
- 单独的 $P_k^B$ 也没有意义
    
- **只有 $(P_k^A, P_k^B)$ 才构成一个“候选接触 patch”**
    

---

# **那它和“三角形对”有什么区别？**

这是一个**层级升级**的问题：

|层级|几何对象|说明|
|---|---|---|
|Level 0|triangle–triangle|BVH 粗筛|
|Level 1|triangle set – triangle set|Step 1/2|
|✅ Level 2|**patch–patch（$P_k$）**|你现在工作的层级|
|Level 3|aggregated patch group|Step 3|

👉 所以：

> **$P_k$ 是“由多个 triangle–triangle 关系聚合而成的局部接触假设”。**

---

# **为什么必须这样定义 P？（这是关键）**

你前面反复提到的问题：

- 不同网格密度
    
- 小三角被多个大三角覆盖
    
- 单对 SR 不公平
    
- 曲面离散导致配对不稳定
    

👉 **这些问题在 triangle 级别是无解的。**

只有当你把研究对象提升为：

> **“一小片区域 vs 一小片区域”**

这些问题才变成：

- 可定义
    
- 可统计
    
- 可比较
    
- 可合并（Step 3）
    

---

# **P 在 Step 2 和 Step 3 中分别扮演什么角色？**

### 在 Step 2

- $P_k$ 是 **最小可信候选单元**
    
- 对它计算：
    
    - SR
        
    - SR_agg
        
    - gap 分布
        
    - 法线统计
        
- 目的：**判断“值不值得继续考虑”**
    

---

### 在 Step 3

- $P_k$ 是 **拓扑扩展的节点**
    
- Patch 之间：
    
    - 通过 $P_k^A$ 或 $P_k^B$ 的邻接关系连接
- 目的：**恢复一个完整公共区域**
    

---

# **一个非常贴近你直觉的比喻（非论文用，但好理解）**

你可以把它理解成：

- 三角形 = 像素
    
- $P_k$ = 超像素（superpixel）
    
- Step 3 = 把属于同一物体的超像素拼起来
    

你之前所有的直觉，其实都是在**不自觉地把问题往 P 这个层级推**。

---

# **一句话定稿（你以后可以直接用）**

> In our formulation, a patch $P_k$ is defined as a paired local surface region $(P_k^A, P_k^B)$ on object A and object B, respectively, which represents a candidate contact region derived from multiple triangle-level interactions. All subsequent support evaluation and aggregation operations are performed on such paired patches rather than on individual triangles.

---